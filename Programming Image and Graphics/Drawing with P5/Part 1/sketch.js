function setup() {
    // Used to create a canvas
    createCanvas(300, 200);
}

function draw() {

    // Assigned the background color to be green
    background(0, 255, 0);
    
    // Created a circle
    circle(80, 100, 100);
    
    // Created a square shape
    square(150, 50, 100); 
}